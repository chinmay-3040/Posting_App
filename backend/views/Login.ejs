<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">

        <div class="w-full h-full p-10 text-white">
            <a href="/" class="text-sm text-zinc-500 mt-3 hover:text-blue-500">back to home</a>
            <p>Not a user? <a href="/create" class="text-sm text-blue-500">create user</a></p>
            
            <div style="display:flex;justify-content:center;align-items: center;">
                
                <form action="/login" method="post"  style="display:flex;flex-direction: column; gap: 20px;" >
                    <h1 class="text-3xl tracking-tighter mb-3 ">Login</h1>

                    <% if (error) { %>
                        <!-- This is where the error message will be displayed -->
                        <div class="text-red-500"><%= error %></div>
                    <% } %>
                    
                    <input class="px-5 py-2 bg-transparent text-white border-2 border-zinc-800 rounded-lg outline:none" name="email" type="text" placeholder="email">
                    <input class="px-5 py-2 bg-transparent text-white border-2 border-zinc-800 rounded-lg outline:none" name="password" type="text" placeholder="password">
                    <input class="px-5 py-2 bg-blue-500 rounded-lg ml-3" type="submit" value="Login">
                </form>
            </div>
        </div>
</body>
</html>