<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">

    <% if(!loggedin){ %>
        <div class="w-full h-full p-10 text-white">
            <a class="text-zinc-900 ml-3 my-3 p-2 bg-zinc-500 mt-2 rounded-lg hover:bg-blue-500" href="/login">Login</a>
            <a class="text-zinc-900 ml-3 my-3 p-2 bg-zinc-500 mt-2 rounded-lg hover:bg-blue-500" href="/create">Register</a>
        </div>
    <%}else{%>
        <div class="w-full h-full p-10 text-white flex" style="justify-content: space-between;">
            
            <a class="text-zinc-900  ml-3 p-2 bg-zinc-500 mt-2 rounded-lg hover:bg-blue-500" href="/profile">My Profile</a>
            <h3 class="text-green-600 ml-3 p-2 mt-2 rounded-lg">Welcome <%= user.username %></h3>
            <a class="text-zinc-900  ml-3 p-2 bg-zinc-500 mt-2 rounded-lg hover:bg-red-500" href="/logout">Logout</a>
        </div>
        
    <%}%>
    
    
    <h1 class="text-3xl m-3 p-2 text-center">Welcome to the posting app</h1>

    <h2 class="text-yellow-500 text-2xl m-5" >All posts</h2>
                <div class="postcontainer m-5"  >
                    <% posts.reverse().forEach(post => {  %>
                        <div class="posts w-1/3 p-5 border-[1px] border-zinc-500 my-3 rounded-lg bg-zinc-800">
                            <h4 class="text-blue-500 mb-2"><%= post.user.username %></h4>
                            <p><%= post.content %></p>
                            
                            <div style="display: flex; justify-content: space-between;">

                                <%if(loggedin){%>
                                    <div class="btns flex gap-4 mt-5">
                                    
                                        <a href="/like/<%= post._id %>" class="text-pink-500">
                                           <%= post.likes.indexOf(user._id) == -1 ? "like" : "dislike" %>
                                        </a>
                                   
                                    <% if(!(user.posts.indexOf(post._id) == -1)){%>
                                        <a href="/edit/<%= post._id %>" class="text-yellow-500">edit</a>
                                        <a href="/delete/<%= post._id %>" class="text-red-500" >delete</a>
                                    <%}%>
                                </div>

                                <%}%>
                                <div class="btns flex gap-4 mt-5">
                                     <small><%= post.likes.length %> likes</small>
                                     <small><%= post.date %> </small>
                                 </div>
                            </div>

                        </div>
                   <% }); %>
                    
                </div>
</body>
</html>